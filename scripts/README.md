1. check_vm_availability: Checks the probability of securing a spot instance
2. upload_data.ps1: Uploads the data to the storage account.
3. run_job.ps1: Submits the training script (`setup_and_train`) to the VM.
4. peek_log.ps1: Prints the last characters of the logfile to check if training is running
